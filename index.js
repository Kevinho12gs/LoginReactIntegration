import React from 'react';
import ReactDOM from 'react-dom';
import './App.css'; // El archivo de estilos
import LoginApp from './LoginApp';

ReactDOM.render(<LoginApp />, document.getElementById('root'));
